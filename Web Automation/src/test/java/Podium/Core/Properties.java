package Podium.Core;

public class Properties {

    public static Browsers browser = Browsers.CHROME;

    public enum Browsers {
        CHROME,
        FIREFOX,
        EDGE,
        SAFARI
    }
}
